# Asset inventory — buddi-first-1000-grow

CAPTURE GAP (logged): real product footage exists in Google Drive (max_walkthrough.mp4 552MB,
Screen Recording 2026-04-06 .mov 220MB, export- FINAL.mp4 112MB — metadata verified) but cannot
be bulk-downloaded from this environment (Drive MCP is content-in-context only; direct HTTP is
proxy-blocked). ALL visuals are therefore clean UI motion-graphics recreations matching the
Buddi app's visual style (dark #0B0E11 + green #2BD97C, from real product screenshots).

No verified backtest figures exist (STAX_API_KEY unset, api.staxlabs.org unreachable):
charts render shape + "Backtest Result" label with NO numeric values.

frame.md is the sole source of truth for color and type.
