# Asset inventory

No assets were captured — staxlabs.org is unreachable from this environment (proxy network policy), so this project runs the no-capture path.

All product visuals (Buddi chat UI, bank-balance card, question chips, strategy rule cards, backtest chart, paper-trade toggle) are hand-built HTML/CSS/SVG recreations designed per frame, following the script's shot descriptions. No logo files exist; the brand is typeset as the wordmark "buddi" in the display font.

No verified brand palette exists — the design system is the `blue-professional` preset's own palette, adopted unmodified into `frame.md` (warm cream canvas, single cobalt accent, Space Grotesk + Inter). `frame.md` is the sole source of truth for color and type.
