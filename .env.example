# Copy to .env and fill in real values. NEVER commit .env.
# META_ACCESS_TOKEN: Meta Marketing API access token (read-only / ads_read scope recommended)
META_ACCESS_TOKEN=
# META_AD_ACCOUNT_ID: Ad account ID including the act_ prefix
META_AD_ACCOUNT_ID=
